# macOS arm64 payload pins are intentionally not populated yet.
#
# The macOS assembler remains disabled until the relocatable Python/OCR stack
# is pinned and verified on Apple Silicon. Never place Windows fallback URLs in
# this file: a missing Mac component must fail the Mac assembly.
